"""In-tree smoke tests for the lag-monitor package.

These exist alongside the canonical test suite under ``tests/unit/monitoring``
so the package is self-contained for vendoring. The canonical test files
under ``tests/`` cover the same surface in more depth.
"""
