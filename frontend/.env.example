# Frontend environment variables (loaded by Vite at build/dev time).
# All values must be prefixed with VITE_ to be exposed to the browser bundle.

# Override the dashboard WebSocket URL. Defaults to ws://127.0.0.1:8877/ws.
# VITE_RUNTIME_WS_URL=ws://127.0.0.1:8877/ws
