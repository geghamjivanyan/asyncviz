/**
 * Playback speed presets — exported as a constant so importing it
 * doesn't break Fast Refresh on the controls component file.
 */

export const REPLAY_SPEED_PRESETS: readonly number[] = [
  0.25,
  0.5,
  1,
  2,
  5,
  10,
  25,
  100,
];
